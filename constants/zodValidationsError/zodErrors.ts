"use client"
export const zodErrors = {
    stringValueError:" value provide as string instead of other data type!",
    nuemaricalValueError:" value provide as nuemarical instead of other data type!", 
    booleanValueError:" value provide as boolean instead of other data type!", 
}